## Icons

아이콘 이미지 바이너리를 생성하는 함수입니다.
Functions that generate icon image binaries.

#### python
```py
ZipPng: bytes
ZipWebp: bytes
AesPng: bytes
AesWebp: bytes
CloudPng: bytes
CloudWebp: bytes
```

#### javascript
```js
class Icons {
    ZipPng: byte[]
    ZipWebp: byte[]
    AesPng: byte[]
    AesWebp: byte[]
    CloudPng: byte[]
    CloudWebp: byte[]
}
```

#### golang
```go
ZipPng []bytes
ZipWebp []bytes
AesPng []bytes
AesWebp []bytes
CloudPng []bytes
CloudWebp []bytes
```

#### java
```java
class Icons {
    byte[] ZipPng
    byte[] ZipWebp
    byte[] AesPng
    byte[] AesWebp
    byte[] CloudPng
    byte[] CloudWebp
}
```
